{
    "name": "finshare-app",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler pages dev",
        "deploy": "wrangler pages deploy"
    },
    "dependencies": {
        "@neondatabase/serverless": "^0.9.0"
    },
    "devDependencies": {
        "wrangler": "^3.0.0"
    }
}